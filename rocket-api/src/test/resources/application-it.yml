spring:
  application:
    name: rocket-api-test

  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: false

logging:
  level:
    root: WARN
    com.rocket: DEBUG
    org.springframework.jdbc: DEBUG